# Copy to .env and set your real MongoDB Atlas connection string here.
# IMPORTANT: do NOT commit your real .env to source control.
# Example (replace <username>,<password>,<cluster>,<dbname> with real values):
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>
MONGODB_URI=mongodb+srv://rohitbiswasiam_db_user:0aC5hHyc2vXooSuL@contacts-app.v590vvh.mongodb.net/contactdb

PORT=5000
DB_MAX_RETRIES=5
DB_RETRY_DELAY_MS=2000
LOG_TO_FILE=false
LOG_DIR=./logs
